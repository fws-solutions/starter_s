// const $ = jQuery.noConflict();

'use strict';

jQuery(function() {
	const BasicBlock = {
		/** @description Cache dom and strings. */


		/** @description Initialize. */
		init: function() {
			console.log('BasicBlock -- WP Block');
		},
	};

	BasicBlock.init();
});
