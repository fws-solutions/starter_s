// const $ = jQuery.noConflict();

'use strict';

jQuery(function() {
	const Banner = {
		/** @description Cache dom and strings. */


		/** @description Initialize. */
		init: function() {
			console.log('Banner -- WP Block');
		}
	};

	Banner.init();
});
